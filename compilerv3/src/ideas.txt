pre procesor:
    input ->
    function entry() {
        return 0;
    }
    lexer:
        output -> 
        [Func_Decl, Ident("entry"), LeftParent, RightParent, LeftSBracket,
        Ident("retrun"), Number(0), SemiColon, RightSBracket]
    parser:
    input ->
    [Func_Decl, Ident("entry"), LeftParent, RightParent, LeftSBracket,
    Ident("retrun"), Number(0), SemiColon, RightSBracket]

    output -> 
    fn[
        name: entry,
        stmts: [
            return: [
                val: 0
            ]
        ]
    ]
asm IrGenerator:
    input ->
    fn[
        name: entry,
        stmt: [
            return: [
                val: 0
            ]
        ]
    ]
    if fn.name = "entry" then fn.name = "_start"
    output ->
    fn entry{
        x=30;
        return 30;
    }
now:
     mainfoo
    /      \
{return
  |
  val: 0 }
